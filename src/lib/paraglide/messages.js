/* eslint-disable */
// This file is generated by scripts/generate-paraglide-wrapper.mjs.
// Do not edit manually.

import { baseLocale, getLocale, isLocale } from "./runtime.js";

const localeLoaders = import.meta.glob("../paraglide-generated/messages/*.js");
const localeCache = new Map();

const cacheModule = (locale, module) => {
  localeCache.set(locale, module);
  return module;
};

import * as baseMessages from "../paraglide-generated/messages/en.js";
cacheModule(baseLocale, baseMessages);

export async function preloadLocaleMessages(locale) {
  const normalized = isLocale(locale) ? locale : baseLocale;
  if (localeCache.has(normalized)) return localeCache.get(normalized);
  const loader = localeLoaders["../paraglide-generated/messages/" + normalized + ".js"];
  if (!loader) return baseMessages;
  const mod = await loader();
  return cacheModule(normalized, mod);
}

const getMessagesForLocale = (locale) => {
  const normalized = isLocale(locale) ? locale : baseLocale;
  const cached = localeCache.get(normalized);
  if (cached) return cached;
  void preloadLocaleMessages(normalized);
  return baseMessages;
};

export const about_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_seo_title(inputs);
};

export const about_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_seo_description(inputs);
};

export const about_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_hero_title(inputs);
};

export const about_hero_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_hero_description(inputs);
};

export const about_values_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_values_label(inputs);
};

export const about_values_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_values_title(inputs);
};

export const about_values_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_values_description(inputs);
};

export const about_value_accuracy_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_accuracy_title(inputs);
};

export const about_value_accuracy_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_accuracy_description(inputs);
};

export const about_value_speed_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_speed_title(inputs);
};

export const about_value_speed_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_speed_description(inputs);
};

export const about_value_objectivity_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_objectivity_title(inputs);
};

export const about_value_objectivity_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_objectivity_description(inputs);
};

export const about_value_built_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_built_title(inputs);
};

export const about_value_built_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_value_built_description(inputs);
};

export const about_founder_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_label(inputs);
};

export const about_founder_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_title(inputs);
};

export const about_founder_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_subtitle(inputs);
};

export const about_founder_name = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_name(inputs);
};

export const about_founder_role = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_role(inputs);
};

export const about_founder_bio_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_bio_1(inputs);
};

export const about_founder_bio_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_bio_2(inputs);
};

export const about_founder_credentials = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_founder_credentials(inputs);
};

export const about_cta_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_cta_title(inputs);
};

export const about_cta_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_cta_description(inputs);
};

export const about_cta_button_primary = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_cta_button_primary(inputs);
};

export const about_cta_button_secondary = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).about_cta_button_secondary(inputs);
};

export const common_home = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_home(inputs);
};

export const common_privacy_policy = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_privacy_policy(inputs);
};

export const common_imprint = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_imprint(inputs);
};

export const common_contact_us = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_contact_us(inputs);
};

export const common_sponsor_us = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_sponsor_us(inputs);
};

export const common_learn_more_sponsor = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_learn_more_sponsor(inputs);
};

export const common_let_me_know = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).common_let_me_know(inputs);
};

export const contact_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_seo_title(inputs);
};

export const contact_breadcrumb = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_breadcrumb(inputs);
};

export const contact_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_hero_title(inputs);
};

export const contact_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_intro(inputs);
};

export const contact_email_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_email_label(inputs);
};

export const contact_subject_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_subject_label(inputs);
};

export const contact_message_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_message_label(inputs);
};

export const contact_send_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_send_button(inputs);
};

export const contact_processing = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_processing(inputs);
};

export const contact_success = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_success(inputs);
};

export const contact_sidebar_pro_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_sidebar_pro_title(inputs);
};

export const contact_sidebar_pro_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_sidebar_pro_desc(inputs);
};

export const contact_sidebar_screener_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_sidebar_screener_title(inputs);
};

export const contact_sidebar_screener_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_sidebar_screener_desc(inputs);
};

export const contact_sidebar_watchlist_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_sidebar_watchlist_title(inputs);
};

export const contact_sidebar_watchlist_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).contact_sidebar_watchlist_desc(inputs);
};

export const cookies_banner_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_banner_title(inputs);
};

export const cookies_banner_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_banner_description(inputs);
};

export const cookies_accept_all = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_accept_all(inputs);
};

export const cookies_reject_all = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_reject_all(inputs);
};

export const cookies_customize = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_customize(inputs);
};

export const cookies_preferences_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_preferences_title(inputs);
};

export const cookies_preferences_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_preferences_description(inputs);
};

export const cookies_necessary_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_necessary_title(inputs);
};

export const cookies_necessary_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_necessary_description(inputs);
};

export const cookies_analytics_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_analytics_title(inputs);
};

export const cookies_analytics_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_analytics_description(inputs);
};

export const cookies_marketing_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_marketing_title(inputs);
};

export const cookies_marketing_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_marketing_description(inputs);
};

export const cookies_save_preferences = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).cookies_save_preferences(inputs);
};

export const data_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_seo_title(inputs);
};

export const data_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_seo_description(inputs);
};

export const data_breadcrumb = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_breadcrumb(inputs);
};

export const data_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_hero_title(inputs);
};

export const data_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_intro(inputs);
};

export const data_contact = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_contact(inputs);
};

export const data_market_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_market_title(inputs);
};

export const data_market_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_market_content(inputs);
};

export const data_coverage_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_coverage_title(inputs);
};

export const data_coverage_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_coverage_content(inputs);
};

export const data_fundamentals_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_fundamentals_title(inputs);
};

export const data_fundamentals_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_fundamentals_content(inputs);
};

export const data_business_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_business_title(inputs);
};

export const data_business_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_business_content(inputs);
};

export const data_analyst_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_analyst_title(inputs);
};

export const data_analyst_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_analyst_content(inputs);
};

export const data_etf_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_etf_title(inputs);
};

export const data_etf_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_etf_content(inputs);
};

export const data_options_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_options_title(inputs);
};

export const data_options_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).data_options_content(inputs);
};

export const donation_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_seo_title(inputs);
};

export const donation_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_seo_description(inputs);
};

export const donation_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_title(inputs);
};

export const donation_intro_greeting = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_intro_greeting(inputs);
};

export const donation_intro_paragraph_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_intro_paragraph_1(inputs);
};

export const donation_intro_paragraph_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_intro_paragraph_2(inputs);
};

export const donation_support_paragraph_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_support_paragraph_1(inputs);
};

export const donation_support_list_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_support_list_intro(inputs);
};

export const donation_support_list_item_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_support_list_item_1(inputs);
};

export const donation_support_list_item_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_support_list_item_2(inputs);
};

export const donation_support_list_item_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_support_list_item_3(inputs);
};

export const donation_cta_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_cta_title(inputs);
};

export const donation_cta_paragraph_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_cta_paragraph_1(inputs);
};

export const donation_cta_paragraph_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_cta_paragraph_2(inputs);
};

export const donation_sidebar_about_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_sidebar_about_title(inputs);
};

export const donation_iframe_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).donation_iframe_title(inputs);
};

export const faq_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_seo_title(inputs);
};

export const faq_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_seo_description(inputs);
};

export const faq_breadcrumb = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_breadcrumb(inputs);
};

export const faq_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_hero_title(inputs);
};

export const faq_q1_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q1_title(inputs);
};

export const faq_q1_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q1_answer(inputs);
};

export const faq_q2_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q2_title(inputs);
};

export const faq_q2_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q2_answer(inputs);
};

export const faq_q3_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q3_title(inputs);
};

export const faq_q3_answer_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q3_answer_1(inputs);
};

export const faq_q3_answer_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q3_answer_2(inputs);
};

export const faq_q3_answer_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q3_answer_3(inputs);
};

export const faq_q4_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q4_title(inputs);
};

export const faq_q4_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q4_answer(inputs);
};

export const faq_q5_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q5_title(inputs);
};

export const faq_q5_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q5_answer(inputs);
};

export const faq_q6_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q6_title(inputs);
};

export const faq_q6_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q6_answer(inputs);
};

export const faq_q7_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q7_title(inputs);
};

export const faq_q7_answer_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q7_answer_1(inputs);
};

export const faq_q7_answer_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q7_answer_2(inputs);
};

export const faq_q8_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q8_title(inputs);
};

export const faq_q8_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q8_answer(inputs);
};

export const faq_q9_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q9_title(inputs);
};

export const faq_q9_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_q9_answer(inputs);
};

export const faq_sidebar_pro_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_sidebar_pro_title(inputs);
};

export const faq_sidebar_pro_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_sidebar_pro_desc(inputs);
};

export const faq_sidebar_about_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).faq_sidebar_about_title(inputs);
};

export const feedback_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_title(inputs);
};

export const feedback_error_signin = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_error_signin(inputs);
};

export const feedback_error_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_error_description(inputs);
};

export const feedback_error_send_try_again = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_error_send_try_again(inputs);
};

export const feedback_success = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_success(inputs);
};

export const feedback_error_send_generic = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_error_send_generic(inputs);
};

export const feedback_description_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_description_label(inputs);
};

export const feedback_page_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_page_label(inputs);
};

export const feedback_cancel_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_cancel_button(inputs);
};

export const feedback_send_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).feedback_send_button(inputs);
};

export const footer_language = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_language(inputs);
};

export const footer_twitter = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_twitter(inputs);
};

export const footer_discord = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_discord(inputs);
};

export const footer_tagline = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_tagline(inputs);
};

export const footer_sections = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_sections(inputs);
};

export const footer_stocks = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_stocks(inputs);
};

export const footer_ipos = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_ipos(inputs);
};

export const footer_etfs = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_etfs(inputs);
};

export const footer_portfolio = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_portfolio(inputs);
};

export const footer_learning_center = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_learning_center(inputs);
};

export const footer_services = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_services(inputs);
};

export const footer_pricing_plan = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_pricing_plan(inputs);
};

export const footer_get_support = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_get_support(inputs);
};

export const footer_website = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_website(inputs);
};

export const footer_login = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_login(inputs);
};

export const footer_faq = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_faq(inputs);
};

export const footer_sitemap = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_sitemap(inputs);
};

export const footer_light = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_light(inputs);
};

export const footer_dark = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_dark(inputs);
};

export const footer_company = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_company(inputs);
};

export const footer_about = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_about(inputs);
};

export const footer_terms_of_use = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_terms_of_use(inputs);
};

export const footer_data_disclaimer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_data_disclaimer(inputs);
};

export const footer_open_source = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).footer_open_source(inputs);
};

export const imprint_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_seo_title(inputs);
};

export const imprint_breadcrumb = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_breadcrumb(inputs);
};

export const imprint_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_hero_title(inputs);
};

export const imprint_tmg_notice = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_tmg_notice(inputs);
};

export const imprint_managing_director = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_managing_director(inputs);
};

export const imprint_managing_director_de = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_managing_director_de(inputs);
};

export const imprint_registered = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_registered(inputs);
};

export const imprint_vat = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_vat(inputs);
};

export const imprint_responsible = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_responsible(inputs);
};

export const imprint_odr_notice = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_odr_notice(inputs);
};

export const imprint_odr_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_odr_content(inputs);
};

export const imprint_odr_note = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_odr_note(inputs);
};

export const imprint_vsbg_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_vsbg_title(inputs);
};

export const imprint_vsbg_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_vsbg_content(inputs);
};

export const imprint_disclaimer_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_disclaimer_title(inputs);
};

export const imprint_content_warning_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_content_warning_title(inputs);
};

export const imprint_content_warning = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_content_warning(inputs);
};

export const imprint_external_links_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_external_links_title(inputs);
};

export const imprint_external_links = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_external_links(inputs);
};

export const imprint_copyright_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_copyright_title(inputs);
};

export const imprint_copyright = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_copyright(inputs);
};

export const imprint_special_conditions_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_special_conditions_title(inputs);
};

export const imprint_special_conditions = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).imprint_special_conditions(inputs);
};

export const login_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_seo_title(inputs);
};

export const login_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_seo_description(inputs);
};

export const login_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_title(inputs);
};

export const login_title_logged_in = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_title_logged_in(inputs);
};

export const login_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_subtitle(inputs);
};

export const login_email_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_email_label(inputs);
};

export const login_password_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_password_label(inputs);
};

export const login_forgot_password = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_forgot_password(inputs);
};

export const login_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_button(inputs);
};

export const login_button_loading = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_button_loading(inputs);
};

export const login_divider = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_divider(inputs);
};

export const login_no_account = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_no_account(inputs);
};

export const login_signup_link = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_signup_link(inputs);
};

export const login_logged_in_as = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_logged_in_as(inputs);
};

export const login_logout_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_logout_button(inputs);
};

export const login_toast_verify_email = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_toast_verify_email(inputs);
};

export const login_toast_success = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_toast_success(inputs);
};

export const login_toast_invalid = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).login_toast_invalid(inputs);
};

export const market_news_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_seo_title(inputs);
};

export const market_news_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_seo_description(inputs);
};

export const market_news_seo_keywords = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_seo_keywords(inputs);
};

export const market_news_structured_name = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_structured_name(inputs);
};

export const market_news_structured_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_structured_description(inputs);
};

export const market_news_structured_item_list_name = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_structured_item_list_name(inputs);
};

export const market_news_structured_item_list_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_structured_item_list_description(inputs);
};

export const market_news_general_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_general_seo_title(inputs);
};

export const market_news_general_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_general_seo_description(inputs);
};

export const market_news_press_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_press_seo_title(inputs);
};

export const market_news_press_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_press_seo_description(inputs);
};

export const market_news_pagination_previous = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_pagination_previous(inputs);
};

export const market_news_pagination_next = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_pagination_next(inputs);
};

export const market_news_pagination_page_of = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_pagination_page_of(inputs);
};

export const market_news_rows_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_rows_label(inputs);
};

export const market_news_back_to_top = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_back_to_top(inputs);
};

export const market_news_upgrade_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_upgrade_label(inputs);
};

export const market_news_pro_subscription_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_pro_subscription_title(inputs);
};

export const market_news_upgrade_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_upgrade_description(inputs);
};

export const market_news_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_label(inputs);
};

export const market_news_more_market_news = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_more_market_news(inputs);
};

export const market_news_stock_news_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_stock_news_label(inputs);
};

export const market_news_more_stock_news = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_more_stock_news(inputs);
};

export const market_news_time_minute = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_minute(inputs);
};

export const market_news_time_minutes = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_minutes(inputs);
};

export const market_news_time_hour = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_hour(inputs);
};

export const market_news_time_hours = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_hours(inputs);
};

export const market_news_time_day = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_day(inputs);
};

export const market_news_time_days = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_days(inputs);
};

export const market_news_time_ago = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_time_ago(inputs);
};

export const market_news_image_alt = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_image_alt(inputs);
};

export const market_news_stocks_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).market_news_stocks_label(inputs);
};

export const pricing_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_seo_title(inputs);
};

export const pricing_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_seo_description(inputs);
};

export const pricing_investors = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_investors(inputs);
};

export const pricing_trustpilot = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_trustpilot(inputs);
};

export const pricing_reviews = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_reviews(inputs);
};

export const pricing_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_hero_title(inputs);
};

export const pricing_hero_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_hero_subtitle(inputs);
};

export const pricing_free_trial = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_free_trial(inputs);
};

export const pricing_cancel_anytime = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_cancel_anytime(inputs);
};

export const pricing_no_hidden_fees = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_no_hidden_fees(inputs);
};

export const pricing_monthly = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_monthly(inputs);
};

export const pricing_annual = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_annual(inputs);
};

export const pricing_save = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_save(inputs);
};

export const pricing_per_month = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_per_month(inputs);
};

export const pricing_basic_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_basic_title(inputs);
};

export const pricing_basic_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_basic_subtitle(inputs);
};

export const pricing_plus_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_plus_title(inputs);
};

export const pricing_plus_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_plus_subtitle(inputs);
};

export const pricing_pro_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_pro_title(inputs);
};

export const pricing_pro_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_pro_subtitle(inputs);
};

export const pricing_feature_credits_10 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_credits_10(inputs);
};

export const pricing_feature_credits_150 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_credits_150(inputs);
};

export const pricing_feature_credits_1000 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_credits_1000(inputs);
};

export const pricing_feature_watchlist_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_watchlist_1(inputs);
};

export const pricing_feature_watchlist_unlimited = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_watchlist_unlimited(inputs);
};

export const pricing_feature_watchlist_pro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_watchlist_pro(inputs);
};

export const pricing_feature_portfolio_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_portfolio_1(inputs);
};

export const pricing_feature_portfolio_unlimited = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_portfolio_unlimited(inputs);
};

export const pricing_feature_portfolio_pro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_portfolio_pro(inputs);
};

export const pricing_feature_alerts_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_alerts_3(inputs);
};

export const pricing_feature_alerts_unlimited = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_alerts_unlimited(inputs);
};

export const pricing_feature_notification = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_notification(inputs);
};

export const pricing_feature_hedgefund = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_hedgefund(inputs);
};

export const pricing_feature_congress = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_congress(inputs);
};

export const pricing_feature_history = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_history(inputs);
};

export const pricing_feature_screener = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_screener(inputs);
};

export const pricing_feature_screener_unlimited = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_screener_unlimited(inputs);
};

export const pricing_feature_download = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_download(inputs);
};

export const pricing_feature_download_unlimited = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_download_unlimited(inputs);
};

export const pricing_feature_no_ads = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_no_ads(inputs);
};

export const pricing_feature_options_historical = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_options_historical(inputs);
};

export const pricing_feature_options_flow = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_options_flow(inputs);
};

export const pricing_feature_unusual_orders = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_unusual_orders(inputs);
};

export const pricing_feature_discord = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_discord(inputs);
};

export const pricing_feature_everything_plus = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_everything_plus(inputs);
};

export const pricing_feature_options_realtime = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_feature_options_realtime(inputs);
};

export const pricing_get_registered = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_get_registered(inputs);
};

export const pricing_get_plus = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_get_plus(inputs);
};

export const pricing_upgrade_plus = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_upgrade_plus(inputs);
};

export const pricing_current_plan = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_current_plan(inputs);
};

export const pricing_upgrade_pro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_upgrade_pro(inputs);
};

export const pricing_unlock_pro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_unlock_pro(inputs);
};

export const pricing_start_trial = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_start_trial(inputs);
};

export const pricing_subscribed_notice = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_subscribed_notice(inputs);
};

export const pricing_manage_subscription = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_manage_subscription(inputs);
};

export const pricing_faq_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_title(inputs);
};

export const pricing_faq_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_subtitle(inputs);
};

export const pricing_faq_q1_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q1_title(inputs);
};

export const pricing_faq_q1_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q1_answer(inputs);
};

export const pricing_faq_q2_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q2_title(inputs);
};

export const pricing_faq_q2_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q2_answer(inputs);
};

export const pricing_faq_q3_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q3_title(inputs);
};

export const pricing_faq_q3_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q3_answer(inputs);
};

export const pricing_faq_q4_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q4_title(inputs);
};

export const pricing_faq_q4_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q4_answer(inputs);
};

export const pricing_faq_q5_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q5_title(inputs);
};

export const pricing_faq_q5_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q5_answer(inputs);
};

export const pricing_faq_q6_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q6_title(inputs);
};

export const pricing_faq_q6_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q6_answer(inputs);
};

export const pricing_faq_q7_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q7_title(inputs);
};

export const pricing_faq_q7_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q7_answer(inputs);
};

export const pricing_faq_q8_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q8_title(inputs);
};

export const pricing_faq_q8_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q8_answer(inputs);
};

export const pricing_faq_q9_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q9_title(inputs);
};

export const pricing_faq_q9_answer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_faq_q9_answer(inputs);
};

export const pricing_upgrade_modal_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_upgrade_modal_title(inputs);
};

export const pricing_upgrade_modal_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_upgrade_modal_content(inputs);
};

export const pricing_upgrade_modal_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_upgrade_modal_button(inputs);
};

export const pricing_upgrade_modal_response_time = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_upgrade_modal_response_time(inputs);
};

export const pricing_billed_annually_120 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_billed_annually_120(inputs);
};

export const pricing_billed_annually_360 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_billed_annually_360(inputs);
};

export const pricing_pro_active = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).pricing_pro_active(inputs);
};

export const privacy_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_seo_title(inputs);
};

export const privacy_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_seo_description(inputs);
};

export const privacy_breadcrumb = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_breadcrumb(inputs);
};

export const privacy_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_hero_title(inputs);
};

export const privacy_last_updated = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_last_updated(inputs);
};

export const privacy_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_intro(inputs);
};

export const privacy_cookies_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_title(inputs);
};

export const privacy_cookies_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_intro(inputs);
};

export const privacy_cookies_necessary = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_necessary(inputs);
};

export const privacy_cookies_necessary_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_necessary_desc(inputs);
};

export const privacy_cookies_analytics = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_analytics(inputs);
};

export const privacy_cookies_analytics_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_analytics_desc(inputs);
};

export const privacy_cookies_marketing = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_marketing(inputs);
};

export const privacy_cookies_marketing_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_marketing_desc(inputs);
};

export const privacy_cookies_manage = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_manage(inputs);
};

export const privacy_cookies_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_cookies_button(inputs);
};

export const privacy_links_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_links_title(inputs);
};

export const privacy_links_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_links_content(inputs);
};

export const privacy_security_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_security_title(inputs);
};

export const privacy_security_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_security_content(inputs);
};

export const privacy_gdpr_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_title(inputs);
};

export const privacy_gdpr_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_intro(inputs);
};

export const privacy_gdpr_contact = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_contact(inputs);
};

export const privacy_gdpr_rights_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_rights_intro(inputs);
};

export const privacy_gdpr_right_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_right_1(inputs);
};

export const privacy_gdpr_right_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_right_2(inputs);
};

export const privacy_gdpr_right_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_right_3(inputs);
};

export const privacy_gdpr_right_4 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_right_4(inputs);
};

export const privacy_gdpr_right_5 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_right_5(inputs);
};

export const privacy_gdpr_right_6 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_right_6(inputs);
};

export const privacy_gdpr_verify = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_verify(inputs);
};

export const privacy_gdpr_complaint = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_gdpr_complaint(inputs);
};

export const privacy_caloppa_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_title(inputs);
};

export const privacy_caloppa_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_intro(inputs);
};

export const privacy_caloppa_compliance = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_compliance(inputs);
};

export const privacy_caloppa_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_1(inputs);
};

export const privacy_caloppa_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_2(inputs);
};

export const privacy_caloppa_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_3(inputs);
};

export const privacy_caloppa_4 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_4(inputs);
};

export const privacy_caloppa_dnt = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_caloppa_dnt(inputs);
};

export const privacy_children_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_children_title(inputs);
};

export const privacy_children_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_children_content(inputs);
};

export const privacy_changes_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_changes_title(inputs);
};

export const privacy_changes_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).privacy_changes_content(inputs);
};

export const register_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_seo_title(inputs);
};

export const register_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_seo_description(inputs);
};

export const register_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_title(inputs);
};

export const register_title_logged_in = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_title_logged_in(inputs);
};

export const register_subtitle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_subtitle(inputs);
};

export const register_email_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_email_label(inputs);
};

export const register_password_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_password_label(inputs);
};

export const register_confirm_password_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_confirm_password_label(inputs);
};

export const register_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_button(inputs);
};

export const register_button_loading = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_button_loading(inputs);
};

export const register_divider = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_divider(inputs);
};

export const register_has_account = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_has_account(inputs);
};

export const register_signin_link = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_signin_link(inputs);
};

export const register_terms_prefix = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_terms_prefix(inputs);
};

export const register_terms_link = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_terms_link(inputs);
};

export const register_terms_middle = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_terms_middle(inputs);
};

export const register_privacy_link = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_privacy_link(inputs);
};

export const register_logged_in_as = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_logged_in_as(inputs);
};

export const register_logout_button = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_logout_button(inputs);
};

export const register_toast_success = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_toast_success(inputs);
};

export const register_toast_invalid = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).register_toast_invalid(inputs);
};

export const sitemap_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_seo_title(inputs);
};

export const sitemap_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_seo_description(inputs);
};

export const sitemap_section_aria_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_section_aria_label(inputs);
};

export const sitemap_breadcrumb_aria_label = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_breadcrumb_aria_label(inputs);
};

export const sitemap_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_title(inputs);
};

export const sitemap_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_intro(inputs);
};

export const sitemap_pages_heading = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_pages_heading(inputs);
};

export const sitemap_articles_heading = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_articles_heading(inputs);
};

export const sitemap_learning_center_heading = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_learning_center_heading(inputs);
};

export const sitemap_financial_terms_heading = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_financial_terms_heading(inputs);
};

export const sitemap_visit_page_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_visit_page_title(inputs);
};

export const sitemap_sidebar_pro_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_sidebar_pro_title(inputs);
};

export const sitemap_sidebar_pro_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_sidebar_pro_desc(inputs);
};

export const sitemap_sidebar_screener_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_sidebar_screener_title(inputs);
};

export const sitemap_sidebar_screener_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_sidebar_screener_desc(inputs);
};

export const sitemap_sidebar_watchlists_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_sidebar_watchlists_title(inputs);
};

export const sitemap_sidebar_watchlists_desc = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_sidebar_watchlists_desc(inputs);
};

export const sitemap_tab_ai_agent = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_ai_agent(inputs);
};

export const sitemap_tab_stocks = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_stocks(inputs);
};

export const sitemap_tab_etfs = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_etfs(inputs);
};

export const sitemap_tab_etf_provider = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_etf_provider(inputs);
};

export const sitemap_tab_options_flow = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_options_flow(inputs);
};

export const sitemap_tab_market_flow = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_market_flow(inputs);
};

export const sitemap_tab_dark_pool_flow = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_dark_pool_flow(inputs);
};

export const sitemap_tab_earnings = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_earnings(inputs);
};

export const sitemap_tab_dividends = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_dividends(inputs);
};

export const sitemap_tab_economic_events = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_economic_events(inputs);
};

export const sitemap_tab_market_news = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_market_news(inputs);
};

export const sitemap_tab_us_senate_portfolio = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_us_senate_portfolio(inputs);
};

export const sitemap_tab_institutional_portfolio = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_institutional_portfolio(inputs);
};

export const sitemap_tab_highest_open_interest = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_highest_open_interest(inputs);
};

export const sitemap_tab_highest_open_interest_change = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_highest_open_interest_change(inputs);
};

export const sitemap_tab_highest_iv_rank = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_highest_iv_rank(inputs);
};

export const sitemap_tab_highest_option_premium = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_highest_option_premium(inputs);
};

export const sitemap_tab_insider_tracker = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_insider_tracker(inputs);
};

export const sitemap_tab_potus_tracker = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_potus_tracker(inputs);
};

export const sitemap_tab_options_calculator = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_options_calculator(inputs);
};

export const sitemap_tab_top_stock_gainers = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_top_stock_gainers(inputs);
};

export const sitemap_tab_top_stock_losers = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_top_stock_losers(inputs);
};

export const sitemap_tab_most_active_stocks = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_most_active_stocks(inputs);
};

export const sitemap_tab_premarket_stock_gainers = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_premarket_stock_gainers(inputs);
};

export const sitemap_tab_premarket_stock_losers = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_premarket_stock_losers(inputs);
};

export const sitemap_tab_afterhours_stock_gainers = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_afterhours_stock_gainers(inputs);
};

export const sitemap_tab_afterhours_stock_losers = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_afterhours_stock_losers(inputs);
};

export const sitemap_tab_watchlist = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_watchlist(inputs);
};

export const sitemap_tab_top_analyst_stock_picks = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_top_analyst_stock_picks(inputs);
};

export const sitemap_tab_top_wallstreet_analysts = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_top_wallstreet_analysts(inputs);
};

export const sitemap_tab_stock_screener = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_stock_screener(inputs);
};

export const sitemap_tab_market_heatmap = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_market_heatmap(inputs);
};

export const sitemap_tab_stock_lists = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_stock_lists(inputs);
};

export const sitemap_tab_price_alerts = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_price_alerts(inputs);
};

export const sitemap_tab_about = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_about(inputs);
};

export const sitemap_tab_contact_us = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_contact_us(inputs);
};

export const sitemap_tab_terms_of_use = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_terms_of_use(inputs);
};

export const sitemap_tab_imprint = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_imprint(inputs);
};

export const sitemap_tab_privacy_policy = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_privacy_policy(inputs);
};

export const sitemap_tab_pricing = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_pricing(inputs);
};

export const sitemap_tab_free_newsletter = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_free_newsletter(inputs);
};

export const sitemap_tab_blog = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_blog(inputs);
};

export const sitemap_tab_data_disclaimer = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_data_disclaimer(inputs);
};

export const sitemap_tab_faq = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_faq(inputs);
};

export const sitemap_tab_advertise_with_us = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).sitemap_tab_advertise_with_us(inputs);
};

export const terms_seo_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_seo_title(inputs);
};

export const terms_seo_description = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_seo_description(inputs);
};

export const terms_breadcrumb = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_breadcrumb(inputs);
};

export const terms_hero_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_hero_title(inputs);
};

export const terms_last_updated = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_last_updated(inputs);
};

export const terms_intro = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_intro(inputs);
};

export const terms_noadvice_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_title(inputs);
};

export const terms_noadvice_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_content_1(inputs);
};

export const terms_noadvice_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_content_2(inputs);
};

export const terms_noadvice_content_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_content_3(inputs);
};

export const terms_noadvice_content_4 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_content_4(inputs);
};

export const terms_noadvice_disclaimer_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_disclaimer_1(inputs);
};

export const terms_noadvice_disclaimer_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_noadvice_disclaimer_2(inputs);
};

export const terms_accuracy_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_accuracy_title(inputs);
};

export const terms_accuracy_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_accuracy_content(inputs);
};

export const terms_content_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_content_title(inputs);
};

export const terms_content_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_content_content(inputs);
};

export const terms_fairuse_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_fairuse_title(inputs);
};

export const terms_fairuse_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_fairuse_content_1(inputs);
};

export const terms_fairuse_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_fairuse_content_2(inputs);
};

export const terms_liability_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_liability_title(inputs);
};

export const terms_liability_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_liability_content_1(inputs);
};

export const terms_liability_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_liability_content_2(inputs);
};

export const terms_copyright_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_copyright_title(inputs);
};

export const terms_copyright_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_copyright_content_1(inputs);
};

export const terms_copyright_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_copyright_content_2(inputs);
};

export const terms_copyright_content_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_copyright_content_3(inputs);
};

export const terms_subscriptions_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_subscriptions_title(inputs);
};

export const terms_subscriptions_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_subscriptions_content_1(inputs);
};

export const terms_subscriptions_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_subscriptions_content_2(inputs);
};

export const terms_subscriptions_content_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_subscriptions_content_3(inputs);
};

export const terms_subscriptions_content_4 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_subscriptions_content_4(inputs);
};

export const terms_subscriptions_content_5 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_subscriptions_content_5(inputs);
};

export const terms_pricing_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_pricing_title(inputs);
};

export const terms_pricing_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_pricing_content(inputs);
};

export const terms_lifetime_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_lifetime_title(inputs);
};

export const terms_lifetime_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_lifetime_content_1(inputs);
};

export const terms_lifetime_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_lifetime_content_2(inputs);
};

export const terms_lifetime_content_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_lifetime_content_3(inputs);
};

export const terms_termination_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_termination_title(inputs);
};

export const terms_termination_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_termination_content_1(inputs);
};

export const terms_termination_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_termination_content_2(inputs);
};

export const terms_termination_content_3 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_termination_content_3(inputs);
};

export const terms_amendments_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_amendments_title(inputs);
};

export const terms_amendments_content = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_amendments_content(inputs);
};

export const terms_data_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_data_title(inputs);
};

export const terms_data_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_data_content_1(inputs);
};

export const terms_data_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_data_content_2(inputs);
};

export const terms_complete_title = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_complete_title(inputs);
};

export const terms_complete_content_1 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_complete_content_1(inputs);
};

export const terms_complete_content_2 = (inputs = {}, options = {}) => {
  const locale = options?.locale ?? getLocale();
  return getMessagesForLocale(locale).terms_complete_content_2(inputs);
};

export const m = {
  about_seo_title,
  about_seo_description,
  about_hero_title,
  about_hero_description,
  about_values_label,
  about_values_title,
  about_values_description,
  about_value_accuracy_title,
  about_value_accuracy_description,
  about_value_speed_title,
  about_value_speed_description,
  about_value_objectivity_title,
  about_value_objectivity_description,
  about_value_built_title,
  about_value_built_description,
  about_founder_label,
  about_founder_title,
  about_founder_subtitle,
  about_founder_name,
  about_founder_role,
  about_founder_bio_1,
  about_founder_bio_2,
  about_founder_credentials,
  about_cta_title,
  about_cta_description,
  about_cta_button_primary,
  about_cta_button_secondary,
  common_home,
  common_privacy_policy,
  common_imprint,
  common_contact_us,
  common_sponsor_us,
  common_learn_more_sponsor,
  common_let_me_know,
  contact_seo_title,
  contact_breadcrumb,
  contact_hero_title,
  contact_intro,
  contact_email_label,
  contact_subject_label,
  contact_message_label,
  contact_send_button,
  contact_processing,
  contact_success,
  contact_sidebar_pro_title,
  contact_sidebar_pro_desc,
  contact_sidebar_screener_title,
  contact_sidebar_screener_desc,
  contact_sidebar_watchlist_title,
  contact_sidebar_watchlist_desc,
  cookies_banner_title,
  cookies_banner_description,
  cookies_accept_all,
  cookies_reject_all,
  cookies_customize,
  cookies_preferences_title,
  cookies_preferences_description,
  cookies_necessary_title,
  cookies_necessary_description,
  cookies_analytics_title,
  cookies_analytics_description,
  cookies_marketing_title,
  cookies_marketing_description,
  cookies_save_preferences,
  data_seo_title,
  data_seo_description,
  data_breadcrumb,
  data_hero_title,
  data_intro,
  data_contact,
  data_market_title,
  data_market_content,
  data_coverage_title,
  data_coverage_content,
  data_fundamentals_title,
  data_fundamentals_content,
  data_business_title,
  data_business_content,
  data_analyst_title,
  data_analyst_content,
  data_etf_title,
  data_etf_content,
  data_options_title,
  data_options_content,
  donation_seo_title,
  donation_seo_description,
  donation_title,
  donation_intro_greeting,
  donation_intro_paragraph_1,
  donation_intro_paragraph_2,
  donation_support_paragraph_1,
  donation_support_list_intro,
  donation_support_list_item_1,
  donation_support_list_item_2,
  donation_support_list_item_3,
  donation_cta_title,
  donation_cta_paragraph_1,
  donation_cta_paragraph_2,
  donation_sidebar_about_title,
  donation_iframe_title,
  faq_seo_title,
  faq_seo_description,
  faq_breadcrumb,
  faq_hero_title,
  faq_q1_title,
  faq_q1_answer,
  faq_q2_title,
  faq_q2_answer,
  faq_q3_title,
  faq_q3_answer_1,
  faq_q3_answer_2,
  faq_q3_answer_3,
  faq_q4_title,
  faq_q4_answer,
  faq_q5_title,
  faq_q5_answer,
  faq_q6_title,
  faq_q6_answer,
  faq_q7_title,
  faq_q7_answer_1,
  faq_q7_answer_2,
  faq_q8_title,
  faq_q8_answer,
  faq_q9_title,
  faq_q9_answer,
  faq_sidebar_pro_title,
  faq_sidebar_pro_desc,
  faq_sidebar_about_title,
  feedback_title,
  feedback_error_signin,
  feedback_error_description,
  feedback_error_send_try_again,
  feedback_success,
  feedback_error_send_generic,
  feedback_description_label,
  feedback_page_label,
  feedback_cancel_button,
  feedback_send_button,
  footer_language,
  footer_twitter,
  footer_discord,
  footer_tagline,
  footer_sections,
  footer_stocks,
  footer_ipos,
  footer_etfs,
  footer_portfolio,
  footer_learning_center,
  footer_services,
  footer_pricing_plan,
  footer_get_support,
  footer_website,
  footer_login,
  footer_faq,
  footer_sitemap,
  footer_light,
  footer_dark,
  footer_company,
  footer_about,
  footer_terms_of_use,
  footer_data_disclaimer,
  footer_open_source,
  imprint_seo_title,
  imprint_breadcrumb,
  imprint_hero_title,
  imprint_tmg_notice,
  imprint_managing_director,
  imprint_managing_director_de,
  imprint_registered,
  imprint_vat,
  imprint_responsible,
  imprint_odr_notice,
  imprint_odr_content,
  imprint_odr_note,
  imprint_vsbg_title,
  imprint_vsbg_content,
  imprint_disclaimer_title,
  imprint_content_warning_title,
  imprint_content_warning,
  imprint_external_links_title,
  imprint_external_links,
  imprint_copyright_title,
  imprint_copyright,
  imprint_special_conditions_title,
  imprint_special_conditions,
  login_seo_title,
  login_seo_description,
  login_title,
  login_title_logged_in,
  login_subtitle,
  login_email_label,
  login_password_label,
  login_forgot_password,
  login_button,
  login_button_loading,
  login_divider,
  login_no_account,
  login_signup_link,
  login_logged_in_as,
  login_logout_button,
  login_toast_verify_email,
  login_toast_success,
  login_toast_invalid,
  market_news_seo_title,
  market_news_seo_description,
  market_news_seo_keywords,
  market_news_structured_name,
  market_news_structured_description,
  market_news_structured_item_list_name,
  market_news_structured_item_list_description,
  market_news_general_seo_title,
  market_news_general_seo_description,
  market_news_press_seo_title,
  market_news_press_seo_description,
  market_news_pagination_previous,
  market_news_pagination_next,
  market_news_pagination_page_of,
  market_news_rows_label,
  market_news_back_to_top,
  market_news_upgrade_label,
  market_news_pro_subscription_title,
  market_news_upgrade_description,
  market_news_label,
  market_news_more_market_news,
  market_news_stock_news_label,
  market_news_more_stock_news,
  market_news_time_minute,
  market_news_time_minutes,
  market_news_time_hour,
  market_news_time_hours,
  market_news_time_day,
  market_news_time_days,
  market_news_time_ago,
  market_news_image_alt,
  market_news_stocks_label,
  pricing_seo_title,
  pricing_seo_description,
  pricing_investors,
  pricing_trustpilot,
  pricing_reviews,
  pricing_hero_title,
  pricing_hero_subtitle,
  pricing_free_trial,
  pricing_cancel_anytime,
  pricing_no_hidden_fees,
  pricing_monthly,
  pricing_annual,
  pricing_save,
  pricing_per_month,
  pricing_basic_title,
  pricing_basic_subtitle,
  pricing_plus_title,
  pricing_plus_subtitle,
  pricing_pro_title,
  pricing_pro_subtitle,
  pricing_feature_credits_10,
  pricing_feature_credits_150,
  pricing_feature_credits_1000,
  pricing_feature_watchlist_1,
  pricing_feature_watchlist_unlimited,
  pricing_feature_watchlist_pro,
  pricing_feature_portfolio_1,
  pricing_feature_portfolio_unlimited,
  pricing_feature_portfolio_pro,
  pricing_feature_alerts_3,
  pricing_feature_alerts_unlimited,
  pricing_feature_notification,
  pricing_feature_hedgefund,
  pricing_feature_congress,
  pricing_feature_history,
  pricing_feature_screener,
  pricing_feature_screener_unlimited,
  pricing_feature_download,
  pricing_feature_download_unlimited,
  pricing_feature_no_ads,
  pricing_feature_options_historical,
  pricing_feature_options_flow,
  pricing_feature_unusual_orders,
  pricing_feature_discord,
  pricing_feature_everything_plus,
  pricing_feature_options_realtime,
  pricing_get_registered,
  pricing_get_plus,
  pricing_upgrade_plus,
  pricing_current_plan,
  pricing_upgrade_pro,
  pricing_unlock_pro,
  pricing_start_trial,
  pricing_subscribed_notice,
  pricing_manage_subscription,
  pricing_faq_title,
  pricing_faq_subtitle,
  pricing_faq_q1_title,
  pricing_faq_q1_answer,
  pricing_faq_q2_title,
  pricing_faq_q2_answer,
  pricing_faq_q3_title,
  pricing_faq_q3_answer,
  pricing_faq_q4_title,
  pricing_faq_q4_answer,
  pricing_faq_q5_title,
  pricing_faq_q5_answer,
  pricing_faq_q6_title,
  pricing_faq_q6_answer,
  pricing_faq_q7_title,
  pricing_faq_q7_answer,
  pricing_faq_q8_title,
  pricing_faq_q8_answer,
  pricing_faq_q9_title,
  pricing_faq_q9_answer,
  pricing_upgrade_modal_title,
  pricing_upgrade_modal_content,
  pricing_upgrade_modal_button,
  pricing_upgrade_modal_response_time,
  pricing_billed_annually_120,
  pricing_billed_annually_360,
  pricing_pro_active,
  privacy_seo_title,
  privacy_seo_description,
  privacy_breadcrumb,
  privacy_hero_title,
  privacy_last_updated,
  privacy_intro,
  privacy_cookies_title,
  privacy_cookies_intro,
  privacy_cookies_necessary,
  privacy_cookies_necessary_desc,
  privacy_cookies_analytics,
  privacy_cookies_analytics_desc,
  privacy_cookies_marketing,
  privacy_cookies_marketing_desc,
  privacy_cookies_manage,
  privacy_cookies_button,
  privacy_links_title,
  privacy_links_content,
  privacy_security_title,
  privacy_security_content,
  privacy_gdpr_title,
  privacy_gdpr_intro,
  privacy_gdpr_contact,
  privacy_gdpr_rights_intro,
  privacy_gdpr_right_1,
  privacy_gdpr_right_2,
  privacy_gdpr_right_3,
  privacy_gdpr_right_4,
  privacy_gdpr_right_5,
  privacy_gdpr_right_6,
  privacy_gdpr_verify,
  privacy_gdpr_complaint,
  privacy_caloppa_title,
  privacy_caloppa_intro,
  privacy_caloppa_compliance,
  privacy_caloppa_1,
  privacy_caloppa_2,
  privacy_caloppa_3,
  privacy_caloppa_4,
  privacy_caloppa_dnt,
  privacy_children_title,
  privacy_children_content,
  privacy_changes_title,
  privacy_changes_content,
  register_seo_title,
  register_seo_description,
  register_title,
  register_title_logged_in,
  register_subtitle,
  register_email_label,
  register_password_label,
  register_confirm_password_label,
  register_button,
  register_button_loading,
  register_divider,
  register_has_account,
  register_signin_link,
  register_terms_prefix,
  register_terms_link,
  register_terms_middle,
  register_privacy_link,
  register_logged_in_as,
  register_logout_button,
  register_toast_success,
  register_toast_invalid,
  sitemap_seo_title,
  sitemap_seo_description,
  sitemap_section_aria_label,
  sitemap_breadcrumb_aria_label,
  sitemap_title,
  sitemap_intro,
  sitemap_pages_heading,
  sitemap_articles_heading,
  sitemap_learning_center_heading,
  sitemap_financial_terms_heading,
  sitemap_visit_page_title,
  sitemap_sidebar_pro_title,
  sitemap_sidebar_pro_desc,
  sitemap_sidebar_screener_title,
  sitemap_sidebar_screener_desc,
  sitemap_sidebar_watchlists_title,
  sitemap_sidebar_watchlists_desc,
  sitemap_tab_ai_agent,
  sitemap_tab_stocks,
  sitemap_tab_etfs,
  sitemap_tab_etf_provider,
  sitemap_tab_options_flow,
  sitemap_tab_market_flow,
  sitemap_tab_dark_pool_flow,
  sitemap_tab_earnings,
  sitemap_tab_dividends,
  sitemap_tab_economic_events,
  sitemap_tab_market_news,
  sitemap_tab_us_senate_portfolio,
  sitemap_tab_institutional_portfolio,
  sitemap_tab_highest_open_interest,
  sitemap_tab_highest_open_interest_change,
  sitemap_tab_highest_iv_rank,
  sitemap_tab_highest_option_premium,
  sitemap_tab_insider_tracker,
  sitemap_tab_potus_tracker,
  sitemap_tab_options_calculator,
  sitemap_tab_top_stock_gainers,
  sitemap_tab_top_stock_losers,
  sitemap_tab_most_active_stocks,
  sitemap_tab_premarket_stock_gainers,
  sitemap_tab_premarket_stock_losers,
  sitemap_tab_afterhours_stock_gainers,
  sitemap_tab_afterhours_stock_losers,
  sitemap_tab_watchlist,
  sitemap_tab_top_analyst_stock_picks,
  sitemap_tab_top_wallstreet_analysts,
  sitemap_tab_stock_screener,
  sitemap_tab_market_heatmap,
  sitemap_tab_stock_lists,
  sitemap_tab_price_alerts,
  sitemap_tab_about,
  sitemap_tab_contact_us,
  sitemap_tab_terms_of_use,
  sitemap_tab_imprint,
  sitemap_tab_privacy_policy,
  sitemap_tab_pricing,
  sitemap_tab_free_newsletter,
  sitemap_tab_blog,
  sitemap_tab_data_disclaimer,
  sitemap_tab_faq,
  sitemap_tab_advertise_with_us,
  terms_seo_title,
  terms_seo_description,
  terms_breadcrumb,
  terms_hero_title,
  terms_last_updated,
  terms_intro,
  terms_noadvice_title,
  terms_noadvice_content_1,
  terms_noadvice_content_2,
  terms_noadvice_content_3,
  terms_noadvice_content_4,
  terms_noadvice_disclaimer_1,
  terms_noadvice_disclaimer_2,
  terms_accuracy_title,
  terms_accuracy_content,
  terms_content_title,
  terms_content_content,
  terms_fairuse_title,
  terms_fairuse_content_1,
  terms_fairuse_content_2,
  terms_liability_title,
  terms_liability_content_1,
  terms_liability_content_2,
  terms_copyright_title,
  terms_copyright_content_1,
  terms_copyright_content_2,
  terms_copyright_content_3,
  terms_subscriptions_title,
  terms_subscriptions_content_1,
  terms_subscriptions_content_2,
  terms_subscriptions_content_3,
  terms_subscriptions_content_4,
  terms_subscriptions_content_5,
  terms_pricing_title,
  terms_pricing_content,
  terms_lifetime_title,
  terms_lifetime_content_1,
  terms_lifetime_content_2,
  terms_lifetime_content_3,
  terms_termination_title,
  terms_termination_content_1,
  terms_termination_content_2,
  terms_termination_content_3,
  terms_amendments_title,
  terms_amendments_content,
  terms_data_title,
  terms_data_content_1,
  terms_data_content_2,
  terms_complete_title,
  terms_complete_content_1,
  terms_complete_content_2
};

